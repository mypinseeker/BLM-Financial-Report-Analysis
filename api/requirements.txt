# Vercel Lambda dependencies (web-only, no PPT generation)
# PPT generation (numpy/matplotlib/python-pptx) available via CLI only
fastapi>=0.109.0
mangum>=0.17.0
jinja2>=3.1.0
supabase>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
pyyaml>=6.0
