# BLM PPT 内容原则补充规范
# PPT Content Principles Supplement

---

## 一、核心原则：每页必须有结论

### 1.1 问题

> 不能只罗列数据。每一页都要把最关键的信息总结出来，把关键结论总结出来。

数据是支撑，结论才是价值。看 PPT 的人是决策者，他需要的不是"收入是 3.09B"，
而是"收入重回增长，批发业务是核心驱动力，但固网仍在失血"。

### 1.2 每页 Slide 的三层结构

```
┌──────────────────────────────────────────────────────┐
│                                                        │
│  📊 数据层（What）                                      │
│     图表、表格、指标卡片                                  │
│     ——回答"情况是什么"                                   │
│                                                        │
├──────────────────────────────────────────────────────┤
│                                                        │
│  🔍 洞察层（So What）                                   │
│     变化分析、归因、对比                                  │
│     ——回答"这意味着什么"                                 │
│                                                        │
├──────────────────────────────────────────────────────┤
│  ═══════════════════════════════════════════════════  │
│  💡 Key Message（品牌色横条，每页底部）                    │
│     一句话结论                                          │
│     ——回答"所以应该怎么做 / 需要关注什么"                  │
│  ═══════════════════════════════════════════════════  │
└──────────────────────────────────────────────────────┘
```

**每一页都必须有 Key Message。没有结论的页面不应该出现在 PPT 中。**

### 1.3 Key Message 的写作标准

好的 Key Message：
- ✅ "移动 ARPU 连续 8 季度提升（€11.8→€12.8），价值经营策略见效，但仍低于德电 €14.2，提升空间仍大"
- ✅ "固网用户持续流失（-380K/年），Cable 被 Fiber 替代是结构性趋势，需加速光纤转型"
- ✅ "德电的核心优势在于规模效应和光纤先发，但 Home Connect 转化率仅 6.3%，这是可以追赶的窗口"

差的 Key Message：
- ❌ "以上是移动业务的主要数据"（废话）
- ❌ "移动业务表现良好"（太模糊）
- ❌ "ARPU 为 €12.8"（只有数据没有判断）

### 1.4 Key Message 的三种类型

根据页面内容的不同，Key Message 侧重也不同：

| 页面类型 | Key Message 侧重 | 示例 |
|---------|-----------------|------|
| 数据分析页 | **判断 + 原因** | "服务收入重回增长(+0.5%)，批发业务贡献显著，但有机增长仍待验证" |
| 对比分析页 | **差距 + 方向** | "EBITDA 落后德电 5.9pp，核心差距在于规模效应和运营效率，需聚焦成本优化" |
| 机会/威胁页 | **行动建议** | "B2B 云安全是最大增量机会(€2.5B TAM)，需在 6 个月内建立专项团队" |
| 趋势分析页 | **趋势判断 + 风险** | "用户净增连续 3 季度放缓（115K→80K），若持续下滑将影响 FY27 收入目标" |

---

## 二、两轮输出：先长后短

### 2.1 流程

```
第一轮：完整版（Draft）
  ├─ 每页都有，信息详尽
  ├─ 数据完整呈现
  ├─ AI 给出所有发现和结论
  ├─ 页数不限（可能 35-45 页）
  │
  ↓
人机共创环节
  ├─ 用户逐页/逐看确认
  ├─ 用户标注：认可 / 质疑 / 补充 / 删除
  ├─ 用户说"这页不重要可以去掉"
  ├─ 用户说"这两页可以合成一页"
  │
  ↓
第二轮：精简版（Final）
  ├─ 只保留用户认可的关键内容
  ├─ 合并冗余页面
  ├─ Key Message 经过用户验证
  ├─ 精简到 20-25 页
  ├─ 这是最终交付物
```

### 2.2 两个版本的区别

| 维度 | 完整版（Draft） | 精简版（Final） |
|------|----------------|----------------|
| 用途 | 供用户审阅和讨论 | 最终交付给客户 |
| 页数 | 35-45 页 | 20-25 页 |
| 可选页 | 全部展开 | 只保留必要的 |
| 细分业务 | 每个业务至少一页 | 重点业务展开，次要业务合并 |
| 对手分析 | 每个对手一页 | 可精简为重点对手展开 + 对比表 |
| 数据密度 | 高（完整数据 + 归因） | 中（关键数据 + 精炼结论） |
| Key Message | AI 初始版本 | 经用户确认/修改的版本 |
| 溯源附录 | 完整 | 可选（看用户需要） |

### 2.3 数据模型

```python
@dataclass
class SlideContent:
    """一页 Slide 的内容"""
    slide_id: str                 # "S08_mobile_deep_dive"
    section: str                  # "look_at_self"
    title: str                    # "移动业务深度分析"
    
    # 三层内容
    data_layer: dict              # 数据和图表
    insight_layer: list[str]      # 洞察分析
    key_message: str              # 底部结论
    
    # 版本控制
    is_optional: bool = False     # 是否可选页
    draft_status: str = "included"  # "included" / "merged" / "removed"
    user_feedback: str = None     # 用户的反馈
    final_key_message: str = None # 用户确认后的 Key Message（如修改）

@dataclass 
class PPTVersion:
    """PPT 版本"""
    version: str                  # "draft" / "final"
    slides: list[SlideContent]
    total_pages: int
    
    def to_final(self, user_decisions: list[dict]) -> 'PPTVersion':
        """根据用户决策生成精简版
        
        user_decisions 示例:
        [
            {"slide_id": "S08_mobile", "action": "keep", "key_message_override": None},
            {"slide_id": "S09_fixed", "action": "keep", "key_message_override": "固网..."},
            {"slide_id": "S11_tv_fmc", "action": "merge_into", "target": "S10_b2b"},
            {"slide_id": "S06_market_detail", "action": "remove"},
            {"slide_id": "S15_macro_detail", "action": "remove"},
        ]
        """
        final_slides = []
        for slide in self.slides:
            decision = next((d for d in user_decisions if d["slide_id"] == slide.slide_id), None)
            
            if decision is None or decision["action"] == "keep":
                if decision and decision.get("key_message_override"):
                    slide.final_key_message = decision["key_message_override"]
                final_slides.append(slide)
            elif decision["action"] == "remove":
                continue
            elif decision["action"] == "merge_into":
                # 合并逻辑
                pass
        
        return PPTVersion(
            version="final",
            slides=final_slides,
            total_pages=len(final_slides) + 4,  # +封面/目录/总结/封底
        )
```

---

## 三、各页 Key Message 示例

以德国 Vodafone 为主角的完整示例：

### Section 0: 开场

| Slide | Key Message |
|-------|------------|
| S04 执行摘要 | "Vodafone Germany 处于战略转折点：服务收入重回增长，但网络和利润率差距仍需 2-3 个季度追赶" |

### 看市场

| Slide | Key Message |
|-------|------------|
| S05 市场变化全景 | "德国电信市场进入'Fiber 替代 Cable'的结构性转型期，Vodafone 的 Cable 优势正在被侵蚀，需尽快确立光纤转型路径" |

### 看自己

| Slide | Key Message |
|-------|------------|
| S07 经营总体 | "整体经营企稳向好，服务收入 +0.5% 是 FY24 以来首次转正，但利润率 36.2% 仍低于行业领先水平" |
| S08 移动业务 | "移动是最健康的业务：ARPU 连升 8 季度，新客 ARPU +21% 验证了价值经营策略，但净增放缓信号需关注" |
| S09 固网宽带 | "固网是最大的失血点：用户连续 8 季度净流失，Cable 被 Fiber 替代的趋势不可逆，光纤转售不是长期解" |
| S10 B2B | "B2B 收入增长 3.2% 是亮点，Skaylink 收购补强了云能力，但与德电 T-Systems 的规模差距仍然巨大" |
| S11 TV/批发 | "批发收入因 1&1 迁入大幅增长，是短期最大的利好；TV 业务平稳但缺乏增长动力" |
| S12 网络深度 | "网络结构性矛盾突出：Cable 覆盖广但天花板低，光纤 75% 依赖转售，5G 覆盖落后 O2 达 8pp" |
| S13 暴露面 | "最大暴露面：1100 万用户迁入后的网络体验压力——竞对已开始利用此点发起攻击" |

### 看对手

| Slide | Key Message |
|-------|------------|
| S14 德电 | "德电是'降维打击'级对手：连续 35 季度增长，但 Fiber Home Connect 转化率仅 6.3%，这是唯一的追赶窗口" |
| S15 O2 | "O2 失去 1&1 批发收入后收入下降，但 5G 覆盖 98% 领先全行业，IoT 增长 47% 表明其在新赛道布局领先" |
| S16 1&1 | "1&1 OpenRAN 先驱者身份值得关注，短期亏损但长期成本结构可能颠覆行业，我们的批发合作窗口有限" |
| S17 横向对比 | "四强格局中 Vodafone 位居第二但差距分化：追德电靠效率，防 O2 靠价值，利用 1&1 转型期争取时间" |

### 看宏观

| Slide | Key Message |
|-------|------------|
| S18 宏观仪表盘 | "天气'多云转晴'：德国经济复苏缓慢，但千兆战略和 EU 复苏基金为网络投资提供了政策顺风" |

### 看机会

| Slide | Key Message |
|-------|------------|
| S20 机会清单 | "三大战略级机会：① B2B 云安全(€2.5B TAM) ② 1&1 窗口期用户争夺 ③ 5G 企业专网；最大威胁是 Fiber 替代 Cable 的不可逆趋势" |

---

## 四、更新到 Slide 模板规范

之前输出件规范中每页 Slide 的描述，需要统一增加 Key Message 区域：

```
每页 Slide 的固定结构：

┌────────────────────────────────────────────────────────────────┐
│ 页眉：标题 + 副标题                                              │
│                                                                 │
│ 内容区域（占页面 75-80%）：                                        │
│   数据层 + 洞察层                                                │
│   （图表/表格/分析文字，具体布局因页而异）                            │
│                                                                 │
├────────────────────────────────────────────────────────────────┤
│ Key Message 区域（占页面底部 10-12%）：                             │
│ ══════════════════════════════════════════════════════════════  │
│ ║  💡 品牌色横条背景，白色文字                                    ║ │
│ ║  一句话结论（30-50字），回答 so what                            ║ │
│ ══════════════════════════════════════════════════════════════  │
│                                                                 │
│ 页脚：数据来源 · 页码                                              │
└────────────────────────────────────────────────────────────────┘
```

### Key Message 横条的视觉规范

```python
def _add_key_message_bar(self, slide, message: str):
    """每页底部的 Key Message 横条"""
    # 品牌色背景横条
    self._add_shape(
        slide,
        left=Inches(0.5),
        top=Inches(6.3),
        width=Inches(12.333),
        height=Inches(0.75),
        color=self.style.primary_color,
    )
    # 白色文字
    self._add_text_box(
        slide,
        left=Inches(0.7),
        top=Inches(6.4),
        width=Inches(12),
        height=Inches(0.55),
        text=f"💡 {message}",
        font_size=self.style.body_size,    # 14pt
        font_color=(255, 255, 255),
        bold=True,
    )
```

---

## 五、代码改造要点

| 改造 | 说明 |
|------|------|
| `_add_key_message_bar()` | 新增方法，每页 Slide 生成后统一调用 |
| 每个 `_add_*_slide()` 方法 | 增加 `key_message` 参数，从分析引擎传入 |
| `FiveLooksAnalyzer` | 每个 look 的输出结构增加 `key_messages: dict[str, str]`，每页一个 |
| `generate()` 方法 | 增加 `version="draft"/"final"` 参数 |
| `generate_final()` 方法 | 新增，接受用户反馈后生成精简版 |
