# Web deployment (Vercel Lambda) — kept slim for cold start performance
# For CLI + full local dev, also install: pip install -r requirements-cli.txt

# Web framework
fastapi>=0.109.0
mangum>=0.17.0
jinja2>=3.1.0

# Cloud / API
supabase>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Analysis output (PPT + charts)
python-pptx>=0.6.21
matplotlib>=3.7.0
numpy>=1.24.0

# Config
pyyaml>=6.0
